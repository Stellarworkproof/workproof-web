# WorkProof Web

Mobile-first responsive MVP for the WorkProof escrow flow.

## Commands

```bash
npm install
npm run dev
npm run build
```

The current UI simulates the Stellar transaction states and is structured for replacing the local state transitions with wallet and Soroban contract calls.
